//Tempelate leteral in JS

let pencilPrice = 10;
let eraserPrice = 30;
console.log("The total price is:", pencilPrice + eraserPrice, "Rupees");

//after using the template literal in js

console.log(`The total price is : ${pencilPrice + eraserPrice}rupees `);
